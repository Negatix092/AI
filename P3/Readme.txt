Deber 3 - Inteligencia Artificial

Grupo conformado por: Nicolás Romero, Kevin Huertas, César Cárdenas y Alex Pérez

Contribuciones respectivas:

Nicolás Romero: Nicolás Romero: Contribuyó a la estructura y diseño del código, incluyendo la definición de la clase Node, los métodos para calcular el costo global (f()) y la función heurística DB (DB()). Participó en la fase de pruebas y revisó el cumplimiento de los requisitos. Planteó el mapa de prueba y el problema a resolver. Se encargó de manejar los resultados y la impresión.

César Cárdenas: Al igual que Nicolás, participó en la estructura y diseño del código, contribuyendo en la definición de la clase Node, la implementación de métodos clave como getBestNode(), getAdjNode(), y buildPath(). Contribuyó a la fase de pruebas y aseguró el cumplimiento de los requisitos. Contribuyo en la implementacion del algoritmo A* search.

Kevin Huertas: Contribuyó al diseño y estructura del código, aportando en la definición de la clase Node y la implementación de métodos como getAdjNodeBD() y suma_costos(). Desempeñó un papel esencial en la fase de pruebas y revisó detenidamente el cumplimiento de los requisitos. Participó en el manejo de nodos alcanzados y resultados.

Alex Pérez: Contribuyó a la estructura y diseño del código, en la definición de la clase Node y la implementación de métodos clave como getPos(), getAdjNode(), y lectura_bd(). Realizó pruebas exhaustivas y verifico si es resolvible. Se encargó de implementar el algoritmo A* search.

MODO DE USO / EJEMPLO DE USO:

-Modificar el código de la línea 248 para escoger el estado del tablero que se desea resolver.

247         # setting an initial state
248         initial_state = [3, 1, 0, 8, 4, 2, 5, 6, 7]


