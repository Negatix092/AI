Deber 2 - Inteligencia Artificial

Grupo conformado por: Nicolás Romero, Kevin Huertas, César Cárdenas y Alex Pérez

Contribuciones respectivas:

Nicolás Romero: Estructura de las clases Problem, Node, Map y PriorityQueue. Definición de los métodos expand(), path_actions(), path_states(), best_first_search(), g(), uniform_cost_search y astar_search(). Implementación de la fase de prueba. Revisión del cumplimiento de los requerimientos del deber 1 y 2. Planteamiento del mapa a utilizar (deber1 = Map(...)). Planteamiento del problema a resolver (d1 = Problem('Ellensburg', 'Havre', map=deber1)). Manejo de los resultados mediante el uso de nodos y de la función print.

César Cárdenas: Estructura de las clases Problem, Node, Map y PriorityQueue. Definición de los métodos expand(), path_actions(), path_states(), best_first_search(), g(), uniform_cost_search y astar_search(). Implementación de la fase de prueba. Revisión del cumplimiento de los requerimientos del deber 1 y 2. Planteamiento del mapa a utilizar (deber1 = Map(...)). Planteamiento del problema a resolver (d1 = Problem('Ellensburg', 'Havre', map=deber1)). Implementación del algoritmo A* search (route = astar_search(d1, h=heuristic_distances)).

Kevin Huertas: Estructura de las clases Problem , Node, Map y PriorityQueue. Definición de los métodos expand(), path_actions(), path_states(), best_first_search(), g(), uniform_cost_search y astar_search(). Implementación de fase de prueba y revisión del cumplimiento de los requerimientos del deber 2. Planteamiento de las heurísticas consideradas para el deber 2 (heuristic_distances ={...}). Manejo de los nodos alcanzados (reached) mediante listas y método values() (rint(f"Reached: {list(reached.values())}")).

Alex Pérez: Estructura de las clases Problem, Node, Map y PriorityQueue. Definición de los métodos expand(), printPriorityQueue(), path_actions(), path_states(), best_first_search(), g(), uniform_cost_search y astar_search(). Implementación de fase de prueba. Revisión del cumplimiento de los requerimientos del deber 1 y 2. Planteamiento del mapa a utilizar (deber1 = Map(...)) y de las heurísticas consideradas para el deber 2 (heuristic_distances ={...}). Manejo de los resultados de frontera (frontier.printPriorityQueue()) y del total de nodos expandidos. 

